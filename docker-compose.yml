services:
  # MySync Server
  mysync-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: mysync-server
    restart: unless-stopped
    environment:
      - SERVER_ADDRESS=:8787
      - DATABASE_URL=/app/data/mysync.db
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET must be set}
      - QUOTA_LIMIT_MB=${QUOTA_LIMIT_MB:-100}
      - TOKEN_TTL=${TOKEN_TTL:-720h}
      - SMTP_HOST=${SMTP_HOST:-}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USERNAME=${SMTP_USERNAME:-}
      - SMTP_PASSWORD=${SMTP_PASSWORD:-}
      - SMTP_FROM=${SMTP_FROM:-noreply@localhost}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:?ALLOWED_ORIGINS must be set}
    volumes:
      - mysync-data:/app/data
    ports:
      - "8787:8787"
    networks:
      - mysync-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8787/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Reverse Proxy (Caddy)
  caddy:
    image: caddy:2-alpine
    container_name: mysync-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - mysync-network
    depends_on:
      - mysync-server
    environment:
      - DOMAIN=${DOMAIN:-localhost}

volumes:
  mysync-data:
    driver: local
  caddy-data:
    driver: local
  caddy-config:
    driver: local

networks:
  mysync-network:
    driver: bridge
